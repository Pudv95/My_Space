/**
 * traditional if else to hai hi ofcourse
 * (Condition) ? true statement : false statement ; {ternary operator bhi hai}
 */

/**
 * Logical Operators
 * || -> OR
 * it accepts the first truthy value or last falsy value
 * For example
 * var a = (5||0||null) // 5 
 * var a = (null || 0 || "") // "";
 * && -> AND works other way around
 * it accepts first falsy value or the last true value
 * For example
 * let a = (5 && 0 && 2)// 0;
 * let a = (6 && "hello" && 1.2) //1.2
 * 
 * !! convert anuything to boolean
 * !!"hello" = true
 * first ! convers to bool and NOTs it then second ! bring back to original boolean value
 * 
 */